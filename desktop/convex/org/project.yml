name: OrgDesktop
options:
  bundleIdPrefix: dev.ohmystack.desktop
  deploymentTarget:
    macOS: "14.0"
  xcodeVersion: "16.0"
packages:
  shared:
    path: ../shared
  swift-cross-ui:
    url: https://github.com/stackotter/swift-cross-ui.git
    branch: main
targets:
  Org:
    type: application
    platform: macOS
    sources:
      - path: Sources
    dependencies:
      - package: shared
        product: DesktopShared
      - package: swift-cross-ui
        product: SwiftCrossUI
      - package: swift-cross-ui
        product: DefaultBackend
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: dev.ohmystack.desktop.org
        SWIFT_VERSION: "6.0"
        MACOSX_DEPLOYMENT_TARGET: "14.0"
        GENERATE_INFOPLIST_FILE: true
  OrgUITests:
    type: bundle.ui-testing
    platform: macOS
    sources:
      - path: UITests
    dependencies:
      - target: Org
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: dev.ohmystack.desktop.org.uitests
        SWIFT_VERSION: "6.0"
        GENERATE_INFOPLIST_FILE: true
schemes:
  Org:
    build:
      targets:
        Org: all
    run:
      config: Debug
    test:
      targets:
        - OrgUITests
  OrgUITests:
    build:
      targets:
        Org: all
    test:
      targets:
        - OrgUITests
