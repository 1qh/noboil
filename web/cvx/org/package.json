{
  "name": "@a/convex-org",
  "private": true,
  "description": "noboil cvx org demo app",
  "type": "module",
  "scripts": {
    "build": "next build --turbo",
    "dev": "bun dev.ts",
    "start": "next start",
    "test:e2e": "CONVEX_TEST_MODE=true bun --filter @a/be-convex dev:once && playwright test --reporter=list",
    "typecheck": "next typegen && tsc --noEmit"
  },
  "dependencies": {
    "@a/config": "workspace:*",
    "@sindresorhus/slugify": "latest"
  },
  "devDependencies": {
    "@a/e2e": "workspace:*"
  }
}
